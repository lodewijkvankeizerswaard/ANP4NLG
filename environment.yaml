name: anp4nlg
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.7
  - pip
  - fairseq
  - sacremoses
  - pytorch
  - wandb
  - gensim
  - nltk
  - pip:
    - tensorboard
    - tqdm
    - tokenizers
    - subword-nmt
